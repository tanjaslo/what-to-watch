export type User = {
  id: number,
  email: string,
  name: string,
  avatarUrl: string,
  token: string,
};

export type ServerUser = {
  id: number,
  email: string,
  name: string,
  'avatar_url': string,
  token: string,
};
